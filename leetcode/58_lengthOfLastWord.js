let lengthOfLastWord = (s) => {
      // s = s.split(' ');
      // let lastIndex = s.length - 1
      // for (let i = lastIndex; i < s.length; i++) {
      //       let lengthOfLastWord = s[i];
      //       console.log(lengthOfLastWord.length);
      // }
}

console.log(lengthOfLastWord("   fly me   to   the moon  "));